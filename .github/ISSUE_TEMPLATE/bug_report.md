---
name: Bug report
about: A demo, build, or benchmark not working as documented
---

**Command run** (e.g. `docker compose run pqlab demo hybrid-tls`):

**What happened / full output:**

**Environment:**
- OS + architecture (e.g. macOS arm64, Ubuntu 24.04 x86_64):
- Docker version (`docker --version`):
- Running via Docker as documented, or a native build?

**If native build:** liboqs version and how it was installed:

